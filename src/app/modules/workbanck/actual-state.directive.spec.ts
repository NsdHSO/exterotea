import { ActualStateDirective } from './actual-state.directive';

describe('ActualStateDirective', () => {
  it('should create an instance', () => {
    const directive = new ActualStateDirective();
    expect(directive).toBeTruthy();
  });
});
