import { SetLabelDirective } from './set-label.directive';

describe('SetLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new SetLabelDirective();
    expect(directive).toBeTruthy();
  });
});
