import { GetMiddleEntityDirective } from './get-middle-entity.directive';

describe('GetMiddleEntityDirective', () => {
  it('should create an instance', () => {
    const directive = new GetMiddleEntityDirective();
    expect(directive).toBeTruthy();
  });
});
